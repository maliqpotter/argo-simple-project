server {
    listen 80;
    server_name _;
    
    location /gameku {
        alias /usr/share/nginx/html/;
        try_files $uri $uri/ /index.html =404;
    }
    
    location = /gameku {
        rewrite ^ /gameku/ permanent;
    }
}